version: "3.8"

services:
  backend:
    build: .
    ports:
      - "8083:8083"
    restart: always

  frontend:
    image: nginx:alpine
    ports:
      - "8080:80"
    volumes:
      - ./index.html:/usr/share/nginx/html/index.html
    depends_on:
      - backend
    restart: always
